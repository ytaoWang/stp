General Information
======================

STP (SubTree Partition) is a simple interface for metadata storage. It aims at providing a mechanism for storing metadata like BigTable or HFile.

Dependencies
======================

Linux Kernel version 2.5.X where X >= 37

Installation
======================

./configure
make
cd bin
./stp
For more details see the file 'INSTALL'
